// Chordex state — re-exports from studio-core
export { useChordStore, ACCENT_COLORS } from '@workspace/studio-core';
export type { Theme, AnimationSpeed, DisplayDensity } from '@workspace/studio-core';
