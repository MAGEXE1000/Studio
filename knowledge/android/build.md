# Reusable Knowledge — Android Native Build Commands

This document contains commands to compile native packages.

---

## Build Targets
- Android package builds target the SDK folder:
  `pnpm run build:android:web`
- Capacitor configuration syncing:
  `npx cap sync android`

Source:
* [package.json](file:///c:/Users/ayuda/Documents/.gemini/antigravity/scratch/Studio/package.json#L7-L10)
